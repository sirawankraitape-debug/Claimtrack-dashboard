{
  "name": "claimtrack",
  "version": "1.0.0",
  "description": "ระบบติดตามยืนยันเคสเคลม — Node.js + SQLite (Cloud ready)",
  "scripts": {
    "start": "node server/server.js",
    "postinstall": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
