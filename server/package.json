{
  "name": "claimtrack-server",
  "version": "1.0.0",
  "description": "ระบบติดตามยืนยันเคสเคลม — Node.js + SQLite (local) / PostgreSQL (cloud)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.11.3"
  },
  "optionalDependencies": {
    "better-sqlite3": "^12.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
